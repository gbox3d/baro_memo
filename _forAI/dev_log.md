# Dev Log

## 목차

- [Entries](#entries)

## Entries

- 2026-08-12: `_forAI/` 문서 세트 생성 (README, inventory, memo, dev_log, plan). 저장소는 `readme.md` 만 있는 초기 상태.
- 2026-08-13 (v0.2.0): 목록에 검색·필터를 넣고 **요약 색인**으로 바꿨다. `?status`(콤마 목록)
  `?q` `?author` `?user` `?limit`/`?offset` `?full=1`, 응답에 `total` 추가. `body` 는 목록에서
  빠지고 `bodyPreview`(200자)+`bodyLength` 로 대체. 운영 보드 4건 기준 9404 → 2077 바이트.
  원인: `server.mjs` 가 `url.searchParams` 를 파싱해 놓고 라우터에 안 넘기고 있었다 — 라우터
  규약을 `(method, pathname, query, body, headers)` 로 바꿨다. 관리자 페이지는 목록 요약만 받고
  펼칠 때 `memos/:id` 로 전문을 받도록 수정. 테스트 52 → 64.
- 2026-08-13 (v0.3.0): **DB 를 외장 볼륨으로 이전**하고 **전문 검색**을 넣었다.
  - DB: `/mnt/data/baro_memo_db/memo.db` (`.env` 의 `MEMO_DB`). 이전 파일은
    `localfiles/memo.db.bak-before-mnt-move` 로 남겨 뒀다 — 확인 뒤 지우면 된다.
  - 검색: SQLite FTS5 외부 콘텐츠 색인(`content='memo'`, `tokenize='trigram'`) + 동기화 트리거
    3개. `?q=` 가 제목·본문 양쪽을 보고 결과마다 `snippet` 이 붙는다. bm25 관련도순 정렬.
    새 의존성 없음 — Node 24.15.0 내장 SQLite 3.51.3 에 FTS5 가 들어 있다.
  - 낱말 여럿은 AND, 문장부호는 인용 구절로 감싸 리터럴 취급(`C++`, `node-gyp` 이 500 이 되지
    않게), 낱말당 3자 미만은 400 `query_too_short`(조용한 0건은 "아무도 안 썼다"로 읽힌다).
  - 테스트 64 → 70. 색인이 생성·갱신·삭제를 따라가는지, 기존 DB 에 색인을 얹으면 backfill 되는지
    포함.
  - 문서: `help/memo.md` 에 Search 절, `readme.md` 에 DB 경로·검색, `_forAI/` 전면 갱신
    (inventory/memo/plan 이 v0.1.0 시점에 멈춰 있었다).
- 2026-08-13 (v0.3.1): 관리자 토큰을 DB 옆으로 옮기고, 관리자 페이지 보드에 쪽 넘김을 넣었다.
  - `.env` 의 `ADMIN_TOKEN_FILE=/mnt/data/baro_memo_db/admin-token` (파일 권한 600). 자격증명이
    자기가 지키는 데이터와 같은 수명이어야 한다 — DB 만 볼륨에 있고 열쇠가 저장소 안에 있으면,
    재클론 뒤 보드와 사용자 토큰은 남고 관리할 수단만 사라진다.
  - `core/admin-token.mjs` 신설. **파일이 정본이고 읽기 실패는 `ADMIN_TOKEN` 으로 내려가지
    않는다** — 몰래 내려가면 운영자가 방금 고친 파일이 아닌 잊은 환경변수로 인증된다. 읽기
    실패는 stderr 한 줄 + 미설정(503) 이다. 프로세스를 죽이지 않는 이유: 관리자 자격증명 하나로
    보드 읽기·쓰기까지 멎을 이유가 없다. 기동 로그에 값이 아니라 **출처 경로**를 찍는다.
  - 관리자 페이지 보드: 한 쪽 10건(`PAGE`), `?limit=10&offset=` 로 받는다. 속성창에 `◀ 1–10 / 25 ▶`.
    마지막 쪽에서 메모가 지워져 offset 이 total 을 넘으면 스스로 한 쪽 당긴다.
  - 테스트 70 → 75 (`admin-token.test.mjs`). 쪽 넘김 경계(0/10/20/30)는 `MemoStore` 로 직접 검산.
  - `pnpm admin:token` 추가. 토큰을 보려고 경로를 외워 `cat` 하는 절차를 없앤다 — 파일이 없으면
    만들고(권한 600, 디렉터리째), `--rotate` 로 교체한다. 새 호스트 첫 배포도 이 한 줄이다.
    서버와 **같은 방식으로** `.env` 의 `ADMIN_TOKEN_FILE` 을 읽는다 — 서버가 읽는 파일과 운영자가
    보는 파일이 갈라지면 이 스크립트는 없느니만 못하다.

- 2026-08-13 (skill): `skills/baro-memo/` 추가 — Claude Code 세션에서 보드를 쓰게 하는 부트스트랩.
  - 세 층이 서로 다른 문제를 푼다는 판단: **CLAUDE.md 가 의무**(항상 컨텍스트에 있다),
    **스킬이 절차**(배포 단위, 필요할 때만 로드), **`/api/help` 가 정본**(서버와 함께 갱신).
    스킬만으로는 "스스로 판단해서 올리기"가 안 된다 — 설명이 매칭돼야 로드되는데, 떠올리지
    못하면 매칭할 기회가 없다. MCP 도 이 문제는 못 푼다(수단을 보여 줄 뿐 의무를 만들지 않는다).
  - MCP 는 보류. API 가 curl 몇 줄이면 되는데 프로토콜 계층을 얹으면 의존성 0 이 깨진다.
    넘어갈 조건: Claude Code 아닌 클라이언트가 붙을 때, 또는 셸 인용부호 사고가 잦아질 때.
    그때는 팀원마다 프로세스를 띄우지 말고 **서버가 직접 `/mcp` 를 여는** 쪽이 맞다.
  - 쓰기는 셸에서 JSON 을 조립하지 말고 파일로 써서 `--data-binary @file` 로 보내게 했다.
    따옴표·역따옴표·개행이 든 본문이 조용히 뭉개지는 걸 막는다.
  - 토큰 id=4 (`paimon`) 발급, `~/.config/baro-memo/env` (600). 첫 실사용으로 v0.2.0→v0.3.1
    계약 변경을 보드 #7 에 올렸다 — 남의 파이프라인을 깨는 변경은 보드에 먼저 올린다는
    그 규칙의 첫 이행이다.
- 2026-08-13 (배포): 스킬을 **서버가 서빙**하게 했다 — `/memo/install.sh`, `/memo/skill/`.
  - 팀원이 확실히 가진 것은 보드 주소 하나뿐이다(깃 접근권은 사람마다 다르다). help 를 서버가
    서빙하는 것과 같은 이유로 스킬도 서버가 준다 — 돌고 있는 서버와 스킬 판이 어긋날 수 없다.
  - 진입점은 `GET /api/help` 하나. 그 문서의 "Wiring yourself up" 절이 설치 한 줄을 주고,
    에이전트가 실행한 뒤 토큰을 물어본다. 사람에게 절차 목록을 내밀지 않는다.
  - `install-skill.sh` 는 **파일만** 만진다(스킬 + CLAUDE.md 표시자 블록). 토큰은 묻지 않는다 —
    사람마다 다르고 셸 히스토리에 남는다. 저장소 안에서 돌리면 심볼릭 링크(개발자), 밖에서
    돌리면 내려받기(팀원). 두 번 돌려도 겹치지 않는다.
  - 토큰 검증은 **아무것도 쓰지 않고** 한다: 빈 본문 POST 는 인증을 통과한 뒤에 거절되므로
    `empty_body` 면 토큰이 맞고 `memo_token_invalid` 면 틀리다. 보드에는 아무것도 안 남는다.
  - `{{ORIGIN}}` 토큰 추가(help-doc). `{{BASE}}` 는 경로 접두사라 문서에 명령줄을 적으면
    `curl /memo/install.sh` 가 되어 실행이 안 됐다. Host 헤더에서 절대 주소를 만든다.
  - 함정: `systemctl reload nginx` 직후의 요청은 옛 워커가 받는다. 404 를 보고 설정을 의심하기
    전에 잠깐 두고 다시 친다.
- 2026-08-14 (문서): `readme.md` 에 **「토큰 확인」** 절을 넣었다 (코드 변경 없음, v0.3.1 유지).
  - 빠져 있던 것은 발급이 아니라 **확인**이었다. 배포 3단계 주석이 "파일이 없으면 만든다" 만
    말해서 *이미 있는 값을 어떻게 보는가* 가 저장소 어디에도 없었다 — `pnpm admin:token` 이
    확인 명령이기도 하다는 사실이 스크립트 머리 주석에만 있었다. 3단계 주석을 "값과 파일 경로를
    찍는다" 로 고치고 절로 넘기는 줄을 달았다.
  - 절은 축을 셋으로 가른다. **값을 보는 곳과 맞는지 보는 곳이 다르다**는 것이 이 절의 요지다:
    관리자 값은 `pnpm admin:token`(값 + 읽어 온 경로 → 기동 로그의 `admin=configured (…)` 와
    대조), 팀원 값은 관리자 페이지(목록은 앞 10자, 고르면 전문 + 복사), 맞는지는 프로브.
  - **잃어버린 사용자 토큰은 재발급 대상이 아니다** — 서버가 값을 보관하므로 관리자 페이지에서
    다시 꺼내면 된다. 재발급은 사람이 바뀌거나 값이 샜을 때 하는 일이다. 이게 안 적혀 있어서
    운영자가 멀쩡한 토큰을 폐기하고 새로 발급할 여지가 있었다.
  - 프로브 두 줄과 거절 표(맞음 / 401 값이 아님 / 503 배포에 0개)를 실었다. 인증이 본문 검사보다
    **먼저** 도는 성질에 기대는 것이라, 그 순서가 바뀌면 이 문서가 거짓이 된다 —
    `memo/routes.mjs` 의 토큰 검사는 `body.user` 검사보다 위에 있어야 한다.
  - 돌고 있는 배포(`localhost:3001`)에 직접 쳐서 네 신호를 다 확인했다: 빈 본문 POST 가
    토큰 없이는 `memo_token_invalid` 401, 유효 토큰이면 `empty_body` 400,
    `GET /api/admin/tokens` 가 관리자 토큰이면 200, 아니면 `admin_token_invalid` 401.
    값은 셸 변수로만 넘겼다(대화록·히스토리).
  - `help/tokens.md` 와 스킬은 손대지 않았다. 거절 코드와 프로브는 이미 거기 있었고, 없던 것은
    **운영자용 국문 절차**뿐이다. 같은 사실을 세 곳에 복제하면 갈라진다.
- 2026-08-14 (관리자 페이지): 시간대 선택·백엔드 판 표시·복사 고침·본문 팝업, 그리고 이 페이지의
  **첫 검사 하네스**. 백엔드는 손대지 않았다(v0.3.1 그대로, API 계약 변화 없음).
  - **시간대**: 서버는 UTC(`toISOString()`)만 저장하고 페이지가 문자열을 잘라 찍고 있어서, 새벽
    1시에 쓴 메모가 전날 16:22 로 보였다. 표시만 지역을 고른다 — 기본은 브라우저가 아는 지역,
    목록은 `Intl.supportedValuesOf("timeZone")` 전체(418개), tooltip 에 원본 UTC 를 같이 적는다.
    자주 쓰는 셋은 지름길로 위에 두되 **전체 목록에서 빼지 않는다** — 중복을 지웠더니 훑는 눈에
    Asia/Tokyo 는 있고 Asia/Seoul 만 없는 목록이 됐다.
  - **백엔드 판**: 제목 옆 `v0.3.1`. 정적 파일에 박지 않고 `GET /api/version` 으로 묻는다 —
    페이지와 백엔드는 따로 늙고, 그 어긋남을 잡으려고 보는 자리에 박아 두면 그때부터 거짓말이다.
  - **하루에 네 번 겪은 하나의 교훈: 브라우저는 우리가 안 보는 실행 환경이다.** 자세한 것은
    `memo.md` 의 "관리자 페이지 — 브라우저라는 실행 환경". 네 결함 모두 화면을 봐야만 보였다:
    빈 `<select>` 가 실오라기로 접혀 컨트롤이 사라짐, 평문 HTTP 라 `navigator.clipboard` 자체가
    없어 복사가 TypeError, `.detail{display:flex}` 가 UA 의 `[hidden]` 을 이겨 속성창이 늘 떠
    있음(폐기 버튼이 눌리는데 조용히 아무 일도 안 함), 그리고 브라우저 캐시.
  - **검사 하네스**: `apps/admin/test/dom-shim.mjs` — `node:vm` 에 최소 DOM 을 심어 `app.js` 를
    그대로 실행한다. index.html 에서 정적 option·버튼 라벨을 읽어 오므로 HTML↔JS 계약이 같이
    걸린다. **shim 이 브라우저보다 친절하면 검사가 거짓말을 한다** — 처음엔 shim 이
    `navigator.clipboard` 를 갖고 있어서 복사 버튼이 초록불인 채로 죽어 있었다. 지금 기본값은
    평문 HTTP 다. CSS 는 여전히 못 보므로 `[hidden]`·`[open]` 계약은 style.css 를 **글자로**
    대조하는 검사를 따로 뒀다(가드를 빼 보고 빨간불이 뜨는 것까지 확인).
  - **복사**: 표준 API → `execCommand` → 값 선택만 해 주기, 세 단계. 확인은 **누른 자리**에
    띄운다(버튼 라벨이 1.5초 "복사됨") — 상태줄은 화면 맨 아래라 휴대폰에서는 누른 손가락 밖이고,
    클립보드는 결과가 눈에 안 보이는 동작이다.
  - **본문 팝업**: `<dialog>` 로 화면의 80%(`80vw × 80vh`), 머리는 붙박이고 본문만 구른다.
    닫기·Esc·가림막·같은 줄 재클릭 넷 다 닫히고 표의 강조도 같이 풀린다. `dialog:not([open])`
    도 UA 규칙이라 `[hidden]` 과 **똑같은 함정**이 있어, display 는 `[open]` 에만 준다.
  - **감사**: 브라우저에서만 터지는 가정을 네 렌즈(보안 컨텍스트·삼켜지는 예외·좁은 화면·검사
    사각지대)로 훑고 발견마다 반박 검증을 붙였다 — 24건 중 10건 확인. 그중 기능이 깨진 넷을
    고쳤다(위의 `[hidden]`, 발급 중복 제출 잠금 + `reset()` 을 성공 뒤로, iOS 자동확대 방지
    `@media (pointer: coarse)` 16px, 발급 폼 줄 접기). 남은 5건은 좁은 화면 배치라 `plan.md`.
  - `/memo/admin/` 에 `Cache-Control: no-store`(deploy 조각). 무빌드라 파일 이름에 판 번호가
    없어 브라우저가 옛 판을 쥐면 서버 잘못처럼 보인다. nginx `-t` 후 reload 했다.
  - 검사 75 → 98(관리자 페이지 23). `readme.md` 의 개수 표기는 세 곳이라 같이 고쳤다.
- 2026-08-14 (v0.4.0): **댓글**. 메모마다 스레드가 붙고, 검색이 그 안까지 훑는다.
  - **왜 본문에 이어 붙이지 않았나**: 귀속이다. 서버가 토큰에서 `user` 를 찍는 것이 이 서비스가
    baro_calrory 에서 분리된 이유인데, 남의 본문에 이어 붙이면 그 문장의 주인을 말할 수 없다.
    덤으로 `PATCH` last-write-wins 에서 남의 글이 사라지는 경로가 하나 줄었다.
  - `comment` 테이블 + `comment_fts`(trigram) + 트리거 셋. **스키마는 `schema.mjs` 한 곳에서**
    세운다 — 목록의 `commentCount` 와 검색이 두 테이블에 걸쳐 있어, 스토어마다 자기 것만 만들면
    `MemoStore` 만 세운 코드(기존 검사 전부가 그랬다)가 no such table 로 죽는다. 필드 검증도
    `fields.mjs` 로 갈라 memo·comment 가 같은 상한을 쓴다.
  - 검색: 두 색인의 적중을 UNION 하고 메모마다 `ROW_NUMBER` 로 한 줄만 남긴다(양쪽에서 걸린
    메모가 결과에 두 번 나오지 않게). 결과에 `matchedIn`(memo|comment)과 그쪽 snippet 이 붙는다 —
    본문에 없는 낱말로 걸린 메모를 받은 소비자가 "검색이 고장 났나"로 읽지 않게.
  - 라우트: `GET/POST /api/memos/:id/comments`, `DELETE .../comments/:commentId`. 삭제는 **경로가
    사실과 맞아야** 한다(다른 메모 밑으로 지우면 404 comment_not_found) — 지워 주면 지운 사람은
    자기가 무엇을 지웠는지 모른다. 수정 경로는 없다(append-only): 인용되는 글이 조용히 바뀌면
    인용이 무의미해진다.
  - `GET /api/memos/:id` 가 `{memo, comments}` 로, 목록·한 건 모두 `commentCount` 를 싣는다.
    둘 다 **가산적**이라 기존 소비자는 그대로 돈다.
  - 관리자 페이지는 **보기만**: 팝업 본문 아래에 댓글이 시간순으로 붙고 목록에 개수 열이 생겼다.
    쓰기는 에이전트의 일이라는 경계를 유지했다(그 페이지가 든 것은 관리자 토큰이지 쓰기 토큰이
    아니라는 실제 이유도 있다).
  - 검사 98 → 112. 스킬(`skills/baro-memo/SKILL.md`)에 "남의 글은 고치지 말고 댓글" 절차를 넣었다.
  - 배포: pm2 재시작 뒤 `/api/version` 0.4.0 확인, 운영 보드에서 댓글 작성 → `?q=` 로
    `matchedIn=comment` 확인 → 삭제 → 색인에서 사라짐까지 왕복으로 검증했다.
- 2026-08-14 (v0.5.0): **읽기도 토큰**. 0.4.0 까지 열려 있던 읽기 문턱을 닫았다.
  - 이유는 노출면이다. 이 배포는 사내망 밖에서 닿는 주소(:22030)로 열려 있고, 게시물에는 경로·
    호스트명·식별자·실패 사례가 그대로 들어 있다. "접수함을 보는 데까지 비밀을 요구하면 접수함이
    아니다"라는 원본의 의도는 사내망 전용일 때의 판단이었다.
  - **관리자 토큰도 읽기로 인정한다.** 관리자 페이지가 든 것이 그 값이라 막으면 운영자가 자기
    보드를 못 읽고, 어차피 스스로 사람 토큰을 발급할 수 있어 막는 것이 연극이다. 대신 **쓰기는
    사람 토큰만** — 관리자 토큰에는 찍을 사람이 없다(403 `admin_token_cannot_write`, 새 코드).
  - `/api/health`·`/api/version`·`/api/help` 는 열어 뒀다. 배포 확인과 "토큰을 어떻게 받나"의
    유일한 입구가 그 자신을 요구할 수는 없다.
  - 토큰 0개일 때 읽기도 503 `no_tokens_issued` 다. 401(네 값이 틀렸다)로 답하면 운영자는 영영
    맞는 값을 찾아 헤맨다 — 쓰기에서 이미 세운 그 구분을 읽기에도 그대로 적용했다.
  - 관리자 토큰 비교(`isAdminToken`)를 `core/admin-token.mjs` 로 올려 admin 라우트와 읽기 문턱이
    같은 구현을 쓴다. 같은 비교가 두 벌이면 한쪽만 고쳐진다.
  - **문서 일이 코드보다 컸다.** help(index·memo·tokens)·readme·스킬·`_forAI/memo.md` 가 전부
    "읽기는 열려 있다"고 적고 있었다. `help-doc.test.mjs` 는 라우트 존재만 강제하고 인증 서술은
    못 본다 — 이런 문장은 grep 으로 훑어야 한다(`"No token"`, `"Reads are open"`, `"읽기는 열려"`).
  - 검사 112 → 113. 기존 검사 다수가 무토큰 GET 을 쓰고 있어서 `rig()` 에 기본 리더 토큰을 넣고,
    문턱 자체를 보는 검사만 `{}` 로 끈다. 새 검사 둘: 무토큰·틀린 토큰 401, 관리자 토큰은 읽기
    200·쓰기 403.
  - 배포: 공지(#21)를 **먼저** 올리고 재시작했다. 확인은 밖에서 — 무토큰 401, 사람 토큰 200,
    관리자 토큰 200, 관리자 POST 403, health/version/help 200.
- 2026-08-14 (v0.6.0): **삭제·수정 이력**. 덮이거나 사라진 값을 별도 테이블에 남긴다.
  - `PATCH` 의 body 는 last-write-wins 이고 `DELETE` 는 되돌릴 수 없다. 여러 세션이 남의 글을
    고치고 지우는 보드에서, 사라진 값이 어디에도 없으면 "그 문단 어디 갔나"에 답할 수 없다.
  - **기록은 스토어 안**(`MemoStore.update/remove`, `CommentStore.remove`)이다. 라우터가 부르는
    구조면 언젠가 한 경로가 빠지고 추적은 그때부터 거짓말이 된다. 그래서 `remove(id, user)` 로
    시그니처가 바뀌었다(라우터가 토큰의 사람을 넘긴다).
  - **`audit.memo_id` 에 외래키를 걸지 않았다.** 걸면 메모 삭제 시 그 삭제 기록이 cascade 로 같이
    지워진다 — 가장 필요한 순간에 없어지는 기록. 검사로 못 박았다.
  - **FTS 색인에 넣지 않는다.** 지운 내용이 `?q=` 로 되살아나면 지운 것이 아니다. 열람은
    `GET /api/admin/audit` 하나, 관리자 토큰 전용 — 항목이 지워진 메모의 전문을 들고 있어서
    사용자 토큰으로 열리면 "지웠다"가 "목록에서만 빠졌다"가 된다.
  - 수정은 **바뀐 칸만** 남긴다(같은 값으로 덮은 PATCH 는 이력이 아니다). 메모 삭제는 본문과
    cascade 로 같이 사라지는 **댓글까지** 남긴다.
  - 함정 하나: `schema.mjs` 의 SQL 은 템플릿 리터럴이라 주석에 역따옴표를 쓰면 문자열이 거기서
    끝난다(`?q=` 를 감쌌다가 SyntaxError). 파일에 그 경고를 박아 뒀다.
  - 검사 113 → 125 (`audit-store.test.mjs` + 관리자 라우트 4). 배포 뒤 밖에서 왕복 확인:
    작성 → 수정 → 삭제 후 이력에 before 가 남고, 사용자 토큰으로는 401, 지운 본문은 검색 0건.
  - 보존 기한은 없다(추가만 한다). `plan.md` 의 Risks 에 단조 증가를 적어 뒀다.
- 2026-08-14 (v0.7.0): **이력 열람을 두 층으로.** 0.6.0 은 이력을 관리자만 볼 수 있었는데,
  그러면 자기 글이 고쳐졌는데 본인만 모르는 상태가 된다.
  - `GET /api/memos/:memoId/history` — 토큰만 있으면 누구나. **사실만** 준다:
    `at`·`actor`·`action`, 수정이면 바뀐 **칸 이름**, 삭제면 글 주인과 함께 사라진 댓글 수,
    댓글 삭제면 그 댓글의 작성자. 덮인 본문·지워진 제목은 없다.
  - 경계는 `audit-store.mjs` 의 `toPublicEntry` 한 곳이다. 이름(귀속)과 칸 이름은 사실이고,
    그 안에 든 글이 내용이다 — 그 선을 함수 하나로 못 박아 두 라우트가 같은 판단을 쓴다.
  - **메모가 없어도 200 이다.** 존재 검사를 붙이면 "그 메모 어디 갔나"라는, 이 축이 존재하는
    바로 그 질문에만 404 로 답하게 된다. 검사로 못 박았다.
  - 관리자 페이지 팝업 아래에 이력 줄이 붙는다(`언제 · 누가 · 무엇을`). 본문·댓글과 함께
    나란히 부른다 — 순서대로 부르면 팝업이 두 번 늦게 채워진다.
  - 검사 125 → 130. 내용이 새지 않는지를 **문자열 포함으로** 확인한다(`before`/`after`/`summary`
    키 부재만 보면 나중에 필드 이름이 바뀔 때 조용히 통과한다).
- 2026-08-14 (관리자 페이지): **팀원 초대 메시지**. 토큰 속성창에 「메시지」 버튼을 넣었다.
  운영자가 하는 일은 "발급해서 사람에게 전달" 인데, 값만 던지면 받은 쪽은 그걸로 뭘 하는지 모른다.
  - 팝업에 그대로 보낼 수 있는 안내문이 뜬다: 이게 뭔지 · **AI 대화창에 붙여 넣을 블록**(설명서
    주소·설치 한 줄·토큰) · 토큰의 성질(사람마다 다름, 읽기에도 필요) · AI 와 쓰는 법 다섯 줄 ·
    영어로 쓰는 이유 · 이력이 남는다는 사실. 백엔드 변경이 없어 판은 0.7.0 그대로다.
  - 주소는 **운영자가 지금 열어 본 주소**에서 만든다(`new URL("../", API)`). 사내망으로 열었으면
    사내망 주소가, 터널로 열었으면 터널 주소가 팀원에게 간다 — help 가 Host 로 ORIGIN 을 만드는
    것과 같은 이유이고, 박아 두면 옮긴 날부터 거짓말이 된다.
  - 관리자 페이지 주소는 **넣지 않는다.** 받는 사람에게는 열리지 않는 문이고, 안내는 그 사람이
    실제로 할 수 있는 것만 담아야 한다(검사로 못 박았다).
  - 복사는 기존 3단계 경로를 그대로 쓴다(평문 HTTP 에는 클립보드 API 가 없다). 막히면 메시지
    칸을 선택 상태로 만들어 준다 — 그 자리를 textarea 로 둔 이유이기도 하다.
  - 검사 130 → 134: 토큰·설명서 주소·설치 줄이 들어 있는지, 관리자 주소가 **안** 들어 있는지,
    복사 두 경로, 여닫이. 문구를 통째로 거는 대신 "무엇이 들어 있어야 하는가" 만 본다.
- 2026-08-14 (v0.7.1): 초대 메시지의 **주소를 설정에서** 가져오고, 메시지를 고칠 수 있게 하고,
  국문/영문을 고르게 했다.
  - `.env` 에 `RELEASE_BASE_URL` (baro_kalory 의 `.env` 와 **같은 이름** — 같은 사실을 두 이름으로
    부르지 않는다). `/api/health` 가 요청의 접두사(X-Forwarded-Prefix)와 합쳐 `boardUrl` 로 준다:
    직접 포트면 `…:22030`, nginx 뒤면 `…:22030/memo`. 원점은 설정이, 경로는 요청이 안다.
  - 이 값이 필요한 이유는 하나다: 운영자가 **사내망 IP 로** 관리자 페이지를 열면 초대장에 사내망
    주소가 실려 나가고, 밖에 있는 팀원은 그 주소로 영영 못 닿는다. 설정이 비면 예전처럼 페이지가
    열린 주소로 떨어진다(검사로 두 경우를 다 건다).
  - 메시지는 이제 **textarea 가 편집 가능**하다. 복사되는 것은 화면의 값이고, 고친 것은
    (토큰, 언어)마다 기억해 둔다 — 실수로 닫아도 다듬은 문장이 안 날아간다. 「다시 만들기」로 원문.
  - 언어 선택(한국어/English)은 localStorage 에 남는다. 영문판은 번역이 아니라 같은 구조로 다시
    썼다(붙여 넣을 블록·왜 쓸 만한지·다섯 줄 사용법).
  - 함정 재발: 템플릿 리터럴 안에서 역따옴표를 쓰면 문자열이 거기서 끝난다. 영문판의
    `open`/`done` 을 감쌌다가 SyntaxError — schema.mjs 에서 겪은 것과 같은 것이다.
  - 검사 134 → 137. 관리자 페이지 판 표시는 `/api/version` 대신 `/api/health` 를 부른다(판과
    건네는 주소를 한 번에 받는다).
- 2026-08-14 (관리자 페이지): 팝업 안에서 **복사가 조용히 실패**하던 것을 고쳤다. 토큰 복사는
  되는데 메시지 복사만 안 되던 이유가 이것이다.
  - 원인: `showModal()` 로 연 모달 **밖은 inert** 다. 복사 폴백이 `document.body` 에 임시
    textarea 를 붙이고 `select()` 하는데, 팝업이 열려 있으면 그 요소를 만질 수 없어
    `execCommand("copy")` 가 false 도 아니고 아무 일도 없이 지나간다.
  - 고침 둘: (1) 대상이 값을 든 칸(textarea/input)이면 **그 칸을 직접** 선택해 복사한다 —
    메시지 팝업이 정확히 그 경우다. (2) 임시 요소가 필요하면 `node.closest("dialog")`,
    즉 **열린 팝업 안**에 붙인다.
  - 확인 문구도 옮겼다. `say()` 는 푸터에 쓰는데 그 자리는 **가림막 뒤**라 팝업이 열려 있으면
    아무것도 안 보인다. 버튼 라벨을 1.5초 "복사됨"으로 바꾼다(토큰 복사에 쓰던 방식을
    `flashButton(selector, label)` 로 일반화해 두 버튼이 같은 것을 쓴다).
  - shim 에 **모달의 inert 성질**을 심었다: 열린 dialog 밖의 요소는 선택되지 않는다. 옛 구현으로
    되돌리면 복사 검사 셋이 빨간불인 것을 확인했다 — 이 성질을 흉내 내지 않았기 때문에 지금까지
    초록불이었다(shim 이 브라우저보다 친절하면 검사가 거짓말을 한다, 또 한 번).
  - 검사 137 → 140.
- 2026-08-14 (초대 메시지 정정 + nginx): 넘겨줄 것은 **주소 하나**인데 메시지가 설치 명령을
  옮겨 적고 있었다. 그리고 그 김에 help 의 설치 주소에서 **포트가 빠지는** 버그를 찾았다.
  - 메시지의 붙여 넣기 블록을 `help` 주소 + 토큰 두 줄로 줄였다. help 문서의 "Wiring yourself
    up" 이 이미 "이 URL 을 받았으면 **네가** 설치해라"까지 지시한다 — 절차를 메시지에 복사하면
    두 벌이 되고 한쪽만 낡는다(스킬이 API 를 재문서화하지 않는 것과 같은 규칙). 검사는 이제
    `install.sh` 가 메시지에 **없어야** 통과한다.
  - 용어는 "AI 에이전트(Claude Code 등) 대화창" 으로 적는다. 받는 사람은 팀원이라 "하네스" 같은
    내부 용어를 쓰지 않는다.
  - **nginx: `proxy_set_header Host $host` → `$http_host`.** `$host` 는 포트를 뗀다. 밖에서
    `:22030` 으로 들어온 요청이 백엔드에는 포트 없는 이름으로 보여서, help 가 만들어 주는 설치
    명령이 `http://gobackdev.iptime.org/memo/install.sh` 였다 — 그대로 실행하면 안 닿는다.
    고친 뒤 밖에서는 `:22030` 이, 사내망에서는 `192.168.0.220` 이 나온다(둘 다 확인).
- 2026-08-14 (문서 마무리): 하루치 작업(v0.4.0→v0.7.1 + 관리자 페이지)을 `_forAI/` 에 반영했다.
  코드 변경 없음, 검사 140개 그대로 통과.
  - 갈라져 있던 판 번호를 맞췄다: README 스냅샷 0.4.0 → 0.7.1, inventory 0.7.0 → 0.7.1.
    **문서의 판 번호는 조용히 낡는 종류다** — 저장소 안에 판을 적는 자리가 넷(두 package.json,
    README 스냅샷, inventory)이고 검사가 거는 것은 앞의 둘뿐이다.
  - inventory 의 백엔드 검사 목록에 `token-store.test.mjs` 가 빠져 있었다("일곱 파일"이라
    적혀 있었고 실제로는 여덟이다). 관리자 페이지 검사 설명에 모달 안 복사·초대 메시지
    내용물 계약·언어 전환을 더했다.
  - `memo.md` 에 절 하나를 새로 세웠다: **「밖에서 닿는 주소 — 절대 주소는 누가 만드는가」.**
    Host/`X-Forwarded-Prefix`/`RELEASE_BASE_URL` 세 조각이 각각 무엇을 아는지, `$host` 가
    포트를 뗀다는 것, 그리고 팀원에게 넘길 것은 주소 하나라는 규칙을 한자리에 모았다 —
    지금까지는 nginx 조각의 주석과 dev_log 에만 흩어져 있어서, 다음 세션이 같은 함정 앞에서
    읽을 곳이 없었다.
  - 「반복 금지」 둘 추가: 밖으로 내보내는 주소를 사내망에서만 확인하지 말 것(사내망은 80 이라
    포트가 떨어져도 멀쩡해 보인다), 절차를 두 곳에 적지 말 것.
  - `memo.md` 목차에 「댓글 축」·「삭제·수정 이력」이 빠져 있던 것도 채웠다.
- 2026-08-15 (v0.8.0): **중요도 점수.** 한 사람이 한 글에 1~5 점을 주고, `?sort=score` 가
  그 합으로 보드를 세운다.
  - **문제는 노이즈가 아니라 순서였다.** `plan.md` 의 목표가 "찾을 수 있어야 한다" 인데, 글은
    단조 증가하고 목록은 최신순 하나뿐이라 반나절을 아껴 준 글과 지나가는 메모가 같은 무게로
    놓인다. 보관(`done` archive)이 목록을 줄이는 답이라면 점수는 **순서를 주는** 답이다.
  - **카운터가 아니라 행이다**(`vote` 테이블 + `UNIQUE(memo_id, user)`). 숫자 한 칸에 +1 하면
    "내가 이미 줬나"에 답할 수 없고(세션이 재시작마다 또 준다), 1인당 상한을 스키마가 못 지키고,
    5점이 한 사람의 확신인지 다섯 사람의 합의인지가 사라진다. 댓글을 본문에 이어 붙이지 않은
    것과 같은 판단이다 — 귀속이 있어야 답할 수 있는 질문이 있다.
  - **PUT 이다.** 쓰는 것이 늘 "내 점수"라 멱등하고, 재시도가 순위를 못 바꾼다. POST 였으면
    "5점 더"로 읽혔을 것이고 그건 열의(熱意)를 재는 설계다. 남의 표를 건드리는 경로는 없다.
  - **이력에 남기지 않는다.** 이력은 *남이* 부순 값을 위한 축인데 여기서 덮이는 것은 늘 자기
    값 하나다. 대신 메모 삭제 이력에는 딸린 표를 싣는다(댓글과 같은 이유).
  - 세 값을 싣는다: `score`(합)·`voters`(사람 수)·`myScore`(내 값). `voters` 를 빼면 5×1 과 1×5 가
    같아 보이고, `myScore` 를 빼면 부르는 쪽이 "이미 줬나"를 모른다. `myScore` 는 **쿼리가 아니라
    토큰**에서 온다 — 쿼리로 받으면 남의 표를 조회하는 길이 열린다.
  - **함정 하나를 검사로 못 박았다**: `my_score` 의 물음표가 SELECT 목록에 있어 검색(q)의 두
    물음표보다 **먼저** 바인딩된다. 순서가 어긋나면 검색어가 사람 이름 자리로 들어가 myScore 가
    조용히 0 이 된다 — `vote-store.test.mjs` 의 "파라미터 순서 회귀" 가 그 자리다.
  - 상한은 코드(`SCORE_LIMIT`)와 스키마(`CHECK`) 양쪽에 있다. 넓히려면 테이블을 다시 세워야
    한다는 경고를 두 파일에 박아 뒀다 — 이미 만들어진 DB 의 CHECK 는 코드를 고쳐도 안 바뀐다.
  - 기본 정렬은 **바꾸지 않았다**. `?sort=` 는 추가된 축이고, 응답 변화도 전부 가산적이다.
  - 관리자 페이지는 **보기만** 한다(관리자 토큰에는 찍을 사람이 없다): 리스트에 중요도 칸,
    팝업에 누가 몇 점을 줬는지, 그리고 최신순/중요도순 선택. 축을 바꾸면 첫 쪽으로 돌아간다 —
    "3쪽"은 정렬이 정하는 자리라, 그대로 두면 방금 고른 순서의 11번째부터 보게 된다.
  - 검사 140 → 161 (`vote-store.test.mjs` 9 + 라우트 7 + 관리자 페이지 5). 운영 DB **사본**에
    새 코드를 얹어 갱신 경로를 먼저 확인했다(25건이 그대로 살아 있고 `vote` 테이블이 선다).
  - **적대적 검토에서 다섯이 나왔고 다섯 다 고쳤다**(후보 11건 중 반박을 견딘 것). 셋은
    이 저장소가 이미 글로 적어 둔 규칙을 새 코드가 어긴 자리였다:
    1. **같은 수치의 SQL 이 두 벌이었다.** `VoteStore.summaryFor` 와 `MemoStore` 의
       `SCORE_SUM`/`VOTER_COUNT`. `COUNT(*)` 를 `COUNT(DISTINCT value)` 로 바꿔도 검사 160개가
       초록불이었다 — **여러 사람이 같은 값을 준 경우**를 아무도 안 만들었기 때문이다(모든
       검사가 4+5, 1+5 처럼 값이 서로 달라 "사람 수"와 "서로 다른 값의 수"가 우연히 같았다).
       `summaryFor`/`scoreOf` 를 지우고 점수 라우트가 `memoStore.get()` 을 쓰게 했다. 관리자
       토큰 비교를 `core` 로 올린 것과 같은 판단이다.
    2. **`score_unavailable` 503 이 404·405 보다 앞에 있었다.** 이력 라우트는 일부러 반대
       순서인데(없는 글 404 → 없는 메서드 405 → 미연결 503) 여기만 뒤집혀서, `PATCH .../score`
       와 `/abc/score` 가 503 을 받았다. 503 은 "운영자가 고치면 된다"는 뜻이라 부르는 쪽은
       자기 오타를 고칠 생각을 못 하고 재시도만 한다.
    3. **관리자 페이지가 기본값일 때도 `&sort=new` 를 보냈다.** nginx 가 워킹트리를 바로
       서빙하고 캐시도 꺼 두었으므로 `git pull` 만으로 브라우저는 새 `app.js` 를 받는다 —
       백엔드는 pm2 재시작 전까지 옛 판이고, 옛 판은 모르는 쿼리 이름을 400 으로 거절한다.
       **배포가 낡았는지 보려고 여는 바로 그 도구에서 보드가 통째로 빈 화면이 될** 뻔했다.
       기본 축이면 파라미터를 안 붙이고, `unknown_param` 힌트를 "백엔드가 이 페이지보다
       낡았습니다" 로 바꿨다.
    4. `help/memo.md` 의 라우트 표가 `GET /api/memos/:id` 를 아직 `{memo, comments}` 로 적고
       있었다(실제로는 `scores` 가 붙는다). `help-doc.test.mjs` 는 **경로만** 양방향으로 걸고
       응답 모양은 안 본다 — 그래서 새어 나갔다.
    5. `clear("")`·`scoreOf("")` 의 빈 사용자 가드를 **지워도 검사가 통과**했다. 저장소가
       `user=''` 인 행을 안 만들기 때문인데, 그 가드는 바로 *저장소가 안 쓴 행*(이관 등)을
       위한 것이라 검사가 그 행을 직접 꽂아야 의미가 있다. 지금은 꽂는다.
  - 교훈 하나를 `memo.md` 「반복 금지」에 올렸다: **검사가 통과한다고 그 검사가 무언가를 지키는
    것은 아니다.** 위의 1·5 는 둘 다 "가정을 어긴 데이터를 아무도 안 만들어서" 통과하고 있었다.
- 2026-08-15 (아티팩트 저장소 · apps/files): 보드 요청 #38(연구실 사이로 2.8GB 릴리스를 나르는
  채널)의 B안. **한 저장소, 두 프로세스**로 앉혔다.
  - **판정이 먼저였다.** 요청이 "빌드가 아니라 판정을 달라"고 명시해서, 반대 논증 셋(보드에
    통합하라 / 기존 것을 재사용하라 / 설계 결함을 찾아라)을 돌린 뒤 #38 에 댓글로 회신했다.
    요지: 바이트는 보드 프로세스에 넣지 않는다, "밖에서 닿는 유일한 서비스"는 착오다(도달성은
    nginx 와 공유기 대역 포워딩의 속성이라 형제 서비스가 그대로 물려받는다), 보드가 기여하는
    것은 정체성(whoami)과 기록뿐이다.
  - **왜 프로세스를 갈랐나**: 보드 서버는 본문을 통째로 메모리에 올린 **뒤** 라우팅한다. 수 GB
    스트리밍은 그 구조에 못 들어가고, 넣으면 라우터 규약을 우회하는 두 번째 서버가 보드 안에
    생긴다. **왜 저장소는 안 갈랐나**: 처음엔 갈랐다가 합쳤다 — 둘의 계약(whoami)이 한 커밋에서
    맞물려야 하고, 운영자가 관리할 물건이 둘로 늘 이유가 없었다. 요청자의 기대도 "파일을 DB 가
    아니라 파일로 따로 두라"였지 저장소를 나누라는 것이 아니었다.
  - **파일은 그냥 파일이다.** DB 에는 장부(세션·받은 구간·완성본 해시)만 있고 바이트는
    `FILES_ROOT/store/<sha256>` 이다. 그래서 nginx 가 alias 로 직접 서빙하고 Range·재개·
    sendfile 이 공짜다 — 이 프로세스는 다운로드 바이트를 만지지 않는다.
  - DWarf(`/home/gblab-dgx-01/works/dwarf`, 클론 제공받음)의 와이어 포맷은 이어받고 신뢰 모델을
    바꿨다: 세션 id 를 **서버가 발급**해 토큰의 사람에 묶고(남의 세션에 청크 주입 차단),
    조각파일+병합 대신 희소 파일 제자리 쓰기(피크 디스크 절반·병합 패스 없음), 수신 길이는
    content-length 프레이밍으로 수신 시점에 검증. DWarf 에는 다운로드 경로가 아예 없었다.
  - 검토(4렌즈·17에이전트)가 13건을 확인했고 반박된 것은 없다. 심각 둘은 같은 뿌리 —
    **finalize 가 "지금"만 보고 시간을 안 봤다**: ① 이미 흐르던 청크의 열린 fd 는 rename 을
    따라가 발행본을 오염시킬 수 있었다(내용 주소의 약속이 깨진다). ② finalize 중 크래시가
    세션을 영구 고착시키고 장부 없는 유령 파일을 남겼다. 진행 계수(동기 블록이 곧 잠금)와
    기동 시 `recover()` 로 막았고, 둘 다 **가드를 빼면 빨간불**인 것까지 확인했다.
  - 검사 165 → 203 (아티팩트 38). 배포 뒤 2.8GB 실물로 확인: LAN 업로드 ~40초, 1GB 에서 죽이고
    재실행 → 정확히 1090519040 바이트부터 재개, 서버측 해시 일치, Range 재개, 밖에서 ~11MB/s
    (병목은 가정용 업링크). 시험 아티팩트는 전부 지웠다.
  - 보드: 공지 #39, #38 에 댓글 둘(판정 · 완료 보고). `/mnt/data/baro_memo_files` 는 DB 볼륨과
    나란히 두었다 — 프로세스를 갈랐어도 디스크는 공유라, 여유 20GiB 예약이 파일 쪽 의무다.
